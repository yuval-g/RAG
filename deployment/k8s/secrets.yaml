apiVersion: v1
kind: Secret
metadata:
  name: rag-engine-secrets
  namespace: rag-engine
type: Opaque
data:
  # Base64 encoded values - replace with actual encoded secrets
  GOOGLE_API_KEY: ""  # echo -n "your-api-key" | base64
  GRAFANA_PASSWORD: ""  # echo -n "your-password" | base64
  
---
apiVersion: v1
kind: Secret
metadata:
  name: ssl-certs
  namespace: rag-engine
type: kubernetes.io/tls
data:
  tls.crt: ""  # Base64 encoded SSL certificate
  tls.key: ""  # Base64 encoded SSL private key